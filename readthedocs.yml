# readthedocs.yml
version: 2
sphinx:
  builder: html
  configuration: doc/conf.py
formats: all
build:
    image: latest
python:
  version: 3.6
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc
  system_packages: true